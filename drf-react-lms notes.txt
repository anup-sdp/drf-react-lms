drf lms + react
reacpt part:
module 32, tausif vi
copy project codes
requirements.txt equivalent is package.json in react(vite)

project root, cmd:
myenv\Scripts\activate -------
cd drf-lms-backend

pip install -r requirements.txt

cd ../react-lms-frontend
cd ../drf-lms-backend
python manage.py makemigrations
python manage.py migrate

python manage.py runserver -------
-----
Frontend dependencies are managed via package.json (no Python venv needed)
in github code package.json includes needed packages and vite.config.js includes tailwind.
open separate terminal.
cd react-lms-frontend
npm install 
npm run dev --------

******************************
myenv\Scripts\activate
cd drf-lms-backend
python manage.py runserver
---
cd react-lms-frontend
npm run dev
******************************
pip install django-cors-headers

---
chrome inspect: application > storage > local storage > token
token shows after login
AuthContext hook
useAuth hook


api doc: http://127.0.0.1:8000/swagger/

(Admin)admin=1234
(Teacher)naim-aB@12345
(Student)anup-aB@12345
(Student)rasel-aB@12345

react dev tools extention, press f12 for inspect, see new componets and profiler tabs.

video2: Working with Category, Product, Invoice | Report, Dashboard Summary Front End

------------------
github upload:
git init
git status
git add .
git status
git commit -m "initial commit"
git status
---
(create bramch in githib)
git branch -M main
git remote add origin https://github.com/anup-sdp/drf-react-lms.git
git push -u origin main
---
git commit -m "update DashboardPage.jsx"
git push -u origin main
---
git add .
git commit -m "convert course-instructor to m2m field"
git push -u origin main
---

------------------


at video4 1:43:30
DashboardPage.jsx
// todo: add delete category, add m2m course-teacher, profile popup cancel button(done),
// useState, useEffect
