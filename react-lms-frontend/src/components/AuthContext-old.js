//previous name: AuthContext.js, this extra file not needed! integrated in AuthContext.jsx
//import { createContext } from "react";

// Create a new context called AuthContext and export it for use in other components
//export const AuthContext = createContext();


/*
createContext is a fundamental React API that allows you to create a context for 
sharing data across your component tree without explicitly passing props through every level.

Provider Component: Wraps part of your component tree - Supplies the context value to all descendants
*/
